const INITIAL_STATE = {
    term: "",
    loading: false,
    error: null
}

export default function locatorReducer(state = INITIAL_STATE, action) {
    switch(action.type){
        default:
            return state;
    }
}